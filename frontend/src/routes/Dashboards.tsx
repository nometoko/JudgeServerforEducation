import { Button } from "@chakra-ui/react";

const DashboardPage = () => {
    return (
        <Button bg="green.400" color="white" _hover={{ bg: "green.600" }}>
            Hover Me
        </Button>
    );
};

export default DashboardPage;
